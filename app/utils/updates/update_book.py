def update_book(book, title, publisher, author,year, bio, pgs_number, total_amount):
  book.set_title(title)
  book.set_publisher(publisher)
  book.set_author(author)
  book.set_year(year)
  book.set_bio(bio)
  book.set_pgs_number(pgs_number)
  book.set_total_amount(total_amount)
